# Civilization_Sim
Simulation of a civilization in python, work in progress.

## Usage

Run the simulator by specifying the number of days to simulate (100 is default in case of no argument):

```bash
./simulator.py <number_of_days>